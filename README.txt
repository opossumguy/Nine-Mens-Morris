Nine Men's Morris (Tkinter GUI) + CPU (v9)

Fix:
- No Repeat now blocks repeat farming even when a move creates TWO mills.
  It remembers ALL mills created on the last mill-forming move.

Also includes:
- Full rules panel
- Winner/Loser tags
- CPU mode + hints + selection toggle

Run:
  python morris_gui.py
